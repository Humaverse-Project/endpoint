{% extends 'base.html.twig' %}

{% block title %}Metier{% endblock %}

{% block body %}
    <h1>Metier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ metier.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ metier.nom }}</td>
            </tr>
            <tr>
                <th>Description_c</th>
                <td>{{ metier.descriptionC }}</td>
            </tr>
            <tr>
                <th>Description_l</th>
                <td>{{ metier.descriptionL }}</td>
            </tr>
            <tr>
                <th>Creation</th>
                <td>{{ metier.creation ? metier.creation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_metier_index') }}">back to list</a>

    <a href="{{ path('app_metier_edit', {'id': metier.id}) }}">edit</a>

    {{ include('metier/_delete_form.html.twig') }}
{% endblock %}
